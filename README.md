# RansomEye

RansomEye is a command-line tool for case lifecycle management, evidence export, integrity verification, database backup, and restore operations.

## Requirements

- Python 3.13 or newer

## Run from source

```powershell
python -m ransomeye.commands --help
```

## Installed CLI

```powershell
ransomeye --help
```
